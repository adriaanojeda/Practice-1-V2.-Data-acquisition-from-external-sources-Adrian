package org.example.control;

public class SqliteWeatherStore {
    //Implementacion de WheaterStore (tiene que tener todos los métodos de WeatherStore)
    private String file;  //Main decide como se llama el fichero
}
